# Changelog

## [Unreleased]

### Changed

- **Breaking:** renamed the import name and PyPI package name from `notebattle` to `funbattle`
  to match the repository name. Anyone doing `import notebattle` or `pip install notebattle`
  must switch to `import funbattle` / `pip install funbattle`.
- The old `notebattle` PyPI package will receive one final release forwarding to `funbattle`
  (manual follow-up by the repo owner).
